require('dotenv').config(); const { createApp } = require('./app'); const { createRuntime } = require('./runtime');
async function main() { const runtime = await createRuntime(); const server = createApp(runtime).listen(runtime.config.port, '0.0.0.0', () => console.log(JSON.stringify({ level: 'info', event: 'api_started', port: runtime.config.port, source: runtime.config.dataSource }))); const shutdown = (signal) => { console.log(JSON.stringify({ level: 'info', event: 'shutdown', signal })); server.close(async () => { await runtime.close(); process.exit(0); }); }; process.on('SIGTERM', () => shutdown('SIGTERM')); process.on('SIGINT', () => shutdown('SIGINT')); }
main().catch((error) => { console.error(JSON.stringify({ level: 'fatal', event: 'startup_failed', message: error.message })); process.exit(1); });
